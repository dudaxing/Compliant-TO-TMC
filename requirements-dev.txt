# Install the hashed runtime requirements.lock separately first.
# Testing/build tooling for HF only; this file does not enable pip hash-checking mode.
pytest==9.1.1
build==1.3.0
setuptools==80.9.0
wheel==0.45.1
